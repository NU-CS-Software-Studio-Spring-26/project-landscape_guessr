<% content_for :title, "Editing game" %>

<div class="md:w-2/3 w-full">
  <h1 class="heading-page">Editing game</h1>

  <%= render "form", game: @game %>

  <%= link_to "Show this game", @game, class: "btn-secondary w-full sm:w-auto mt-2 sm:mt-0 sm:ml-2" %>
  <%= link_to "Back to games", games_path, class: "btn-secondary w-full sm:w-auto mt-2 sm:mt-0 sm:ml-2" %>
</div>
